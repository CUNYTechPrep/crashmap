{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.16.4",
    "@testing-library/react": "^13.0.1",
    "@testing-library/user-event": "^13.5.0",
    "axios": "^0.26.1",
    "env-cmd": "^10.1.0",
    "http-proxy-middleware": "^2.0.6",
    "mapbox-gl": "^2.8.1",
    "react": "^18.0.0",
    "react-bootstrap": "^2.2.3",
    "react-dom": "^18.0.0",
    "react-scripts": "5.0.1",
    "react-select": "^5.3.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "env-cmd -f ../.env.development.local --no-override --silent env-cmd -f ../.env.local --no-override --silent env-cmd -f ../.env.development --no-override --silent env-cmd -f ../.env --no-override --silent react-scripts start",
    "build:staging": "env-cmd -f ../.env.staging.local --no-override --silent env-cmd -f ../.env.local --no-override --silent env-cmd -f ../.env.staging --no-override --silent env-cmd -f ../.env --no-override --silent react-scripts build",
    "build:production": "env-cmd -f ../.env.production.local --no-override --silent env-cmd -f ../.env.local --no-override --silent env-cmd -f ../.env.production --no-override --silent env-cmd -f ../.env --no-override --silent react-scripts build",
    "test": "env-cmd -f ../.env.development.local --no-override --silent env-cmd -f ../.env.local --no-override --silent env-cmd -f ../.env.development --no-override --silent env-cmd -f ../.env --no-override --silent react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
